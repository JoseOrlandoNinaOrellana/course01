#include <iostream>   // std::cout std::cerr std::endl
#include <fstream>    // std::ifstream
#include <vector>     // std::vector<>

// Returns the number of elements greater than the pivot in the vector
int countGreater(const std::vector<int>& data, int pivot = 10) {}
